# [Sub] 設計仕様書

## 1. 概要

2つの入力の差を計算する演算クラス。

## 2. 処理フロー (Forward)

1. 要素が2のリストまたはタプルを受け取る。
2. `x0` と `x1` を減算する。
3. 結果を `y` として返す。

## 3. 処理フロー (Backward)

1. 引数に逆伝播の値 `gy` を受け取る。
2. `gy` と `-gy` を返す。