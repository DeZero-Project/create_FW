# [Add] 設計仕様書

## 1. 概要

2つの入力の和を計算する演算クラス。

## 2. 処理フロー (Forward)

1. 要素が2のリストまたはタプルを受け取る。
2. `x0` と `x1` を加算する。
3. 結果を要素1のタプル `(y,)` として返す。

## 3. 処理フロー (Backward)

1. 引数（勾配）をそのまま返す（加算の微分は1であるため）。
2. 入力が2つのため、2つの勾配を返す。