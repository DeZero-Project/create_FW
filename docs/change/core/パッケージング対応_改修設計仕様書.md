# [dezeroパッケージング対応] 改修設計仕様書

## 1. 改修の目的・背景
- **背景**: [コアクラスや基本機能が完成しFW構築の基盤が整ったことで、フォルダの移行整理が可能と判断したため]
- **目的**: [ファイル/ディレクトリ名の修正とパッケージングに伴うフォルダ整理]

## 2. 変更内容一覧

| ファイル名変更前 | ファイル名変更後 | 変更理由 |
| :--- | :--- | :--- |
| `stage2.py` | `core_simple.py` | ExpとSuquare関数を除くコアロジックを移管 |
| `stage2.py` | `function.py` | ExpとSuquare関数を補助関数として移管 |
| `stage2_test.py` | `variable_test.py` | パッケージングに伴いファイル名を変更 |

## 3. 詳細設計（Before & After）

### 3.1 [インポート方法]
#### 変更前 (Before)
- [同じディレクトリ内で動作確認を行なっていたため、ファイル名のインポートのみで動作が行えていた]

#### 変更後 (After)
- **インポート形式**: `[import sys, os], [sys.path.append(os.path.dirname(os.path.dirname(__file__))]` を冒頭に記載した後dezeroのインポートを行う。

## 4. 影響範囲と整合性
- **関連1**: `design`、`temp` を除く全てのディレクトリ/ファイルに影響あり。


## 5. テスト・検証項目

- [x] 動作確認ファイル、jupyterファイル、testファイルそれぞれでdezeroのインポートが行えるか確認。
- [x] dezeroをインポートしたファイルで正しい計算が行えるか確認。
